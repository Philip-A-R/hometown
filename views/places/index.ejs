<% layout('./layouts/boilerplate') %>
    <div id="cluster-map"></div>
    <h1>All Places</h1>
    <form action="/places/new">
        <button>
            New Place
        </button>
    </form>
    </div>
    <ul>
        <% for (let place of places) {%>
            <div class="card mb-3">
                <div class="row">
                    <div class="col-md-4">

                        <img class="img-fluid" alt="..." src="<%=place.images[0].url%> ">


                    </div>
                    <div class="col md-8">
                        <div class="card-body">
                            <h5 class="card-title">

                                <%=place.name%>
                                    </a>
                            </h5>
                            <p class="card-text">
                                <%= place.description %>
                            </p>
                            <p class="card-text">
                                <small class="text-muted">
                                    <%= place.location %>
                                </small>
                            </p>
                            <a class="btn btn-primary" href="places/<%=place._id%>"> View <%= place.category %> </a>
                        </div>
                    </div>
                </div>
            </div>


            <% } %>
    </ul>

    <script>
        const mapToken = '<%-process.env.MAPBOX_TOKEN%>';
        const places ={features: <%- JSON.stringify(places) %>}
    </script>

    <script src="/javascripts/clusterMap.js"></script>˚s